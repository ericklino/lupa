<link href="../../assets/stylesheets/simple-sidebar.css" rel="stylesheet">
<div id="wrapper">
       <div id="sidebar-wrapper">
           <ul class="sidebar-nav">
               <li class="sidebar-brand">
                   <a href="#">
                    <i class="fa fa-search"></i>   LUPA
                   </a>
               </li>
               <li>
                   <a href="/dispositivos"><i class="fa fa-desktop fa-lg"></i>   Dispositivos</a>
               </li>
               <li>
                   <a href="#"><i class="fa fa-pencil fa-lg"></i>   Avaliação  </a>
               </li>
               <li>
                   <a href="#"><i class="fa fa-book fa-lg"></i>   Relatório</a>
               </li>
               <li>
                   <a href="#"><%= link_to 'Sair', destroy_user_session_path, method: :delete %></a>
               </li>
           </ul>
       </div>


       <div id="page-content-wrapper">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">

                    <% @categories.each do |category| %>
                    <h2><%=category.description%></h2>

                    <% category.questions.each do |question|%>
                      <h4><%=question.enunciation %></h4>
                      <h4><%=question.category.description %></h4>

                    <% question.alternatives.each do |alternative|%>
                      <input type="radio" name="aternative"> <%=alternative.description%>


                    <%end%>
                    <%end%>
                    <%end%>

                    <% @alternatives.each do |alternative|%>
                    <input type="radio" name="aternative"> <%=alternative.description%>

                    <%end%>

                    <p>Lupa é um sistema para o apoio da avaliação da acessibilidade e usabilidade em dispositivos móveis abordando o usuário com necessidade especial visual</p>
                    <p>Make sure to keep all page content within the <code>#page-content-wrapper</code>.</p>

                </div>
            </div>
        </div>
    </div>
     </div>
